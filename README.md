# PHRS Faucet Farm Bot

An automated farming bot for the **Pharos Atlantic Faucet**.
This bot requires a **2Captcha API Key** to solve captchas automatically.

---

## 🧩 1. Register & Get Your 2Captcha API Key

You must create an account on 2Captcha to use the bot.

Register here:
👉 [https://2captcha.com/](https://2captcha.com/)

After creating an account, copy your **API Key** from the 2Captcha dashboard.

---

## 📥 2. Clone the Repository

```sh
git clone https://github.com/Chihiroryuu/PHRS_FAUCHET_FARM.git
cd PHRS_FAUCHET_FARM
```

---

## 📦 3. Install Dependencies

Make sure Python is installed, then run:

```sh
pip install -r requirements.txt
```

---

## 🔑 4. Insert Your 2Captcha API Key

Edit the `key.txt` file:

```sh
nano key.txt
```

Paste your 2Captcha API Key → press **CTRL + S**, then **CTRL + X** to exit.

---

## 📥 5. Download `index.py` (Required File)

`index.py` is **not included** in the repository.
To download it, you must:

1. Join my Discord Server
   👉 [https://discord.gg/3FZVDVA2](https://discord.gg/3FZVDVA2)
2. Open **AIRDROP PORTAL** section
3. Verify your account and obtain the required role
4. Download the `index.py` file from the appropriate channel
5. Upload the `index.py` file into your VPS / the bot folder

Make sure `index.py` is located inside the same folder as the other bot files.

---

## ▶️ 6. Run the Bot

Once `index.py` is in the correct folder, start the bot using:

```sh
python index.py
```

---

## 📦 7. Gather All Farmed Accounts

To merge or collect all generated/farmed accounts:

```sh
python gather.py
```

---

## ✔️ Done

Your bot is now fully ready to run.
# PHRS Faucet Farm Bot

Bot otomatis untuk farming faucet **Pharos Atlantic**.
Bot ini membutuhkan **API Key 2Captcha** untuk menyelesaikan captcha secara otomatis.

---

## 🧩 1. Daftar & Dapatkan API Key 2Captcha

Untuk menjalankan bot, kamu wajib memiliki akun 2Captcha.

Daftar di sini:
👉 [https://2captcha.com/](https://2captcha.com/)

Setelah mendaftar, salin **API Key** dari dashboard 2Captcha.

---

## 📥 2. Clone Repository

```sh
git clone https://github.com/Chihiroryuu/PHRS_FAUCHET_FARM.git
cd PHRS_FAUCHET_FARM
```

---

## 📦 3. Install Dependencies

Pastikan Python sudah terinstall, kemudian jalankan:

```sh
pip install -r requirements.txt
```

---

## 🔑 4. Masukkan API Key 2Captcha

Edit file `key.txt` dengan perintah berikut:

```sh
nano key.txt
```

Masukkan API Key 2Captcha milikmu → tekan **CTRL + S** untuk menyimpan, lalu **CTRL + X** untuk keluar.

---

## 📥 5. Download File `index.py` (Wajib)

File `index.py` **tidak tersedia langsung** di repository.
Untuk mengunduhnya kamu harus:

1. Masuk ke Discord saya
   👉 [https://discord.gg/3FZVDVA2](https://discord.gg/3FZVDVA2)
2. Masuk ke menu **AIRDROP PORTAL**
3. Lakukan verifikasi & dapatkan role yang dibutuhkan
4. Download file `index.py` dari channel yang tersedia
5. Upload file `index.py` ke VPS / folder bot ini berada

Pastikan `index.py` berada di folder yang sama dengan file lainnya.

---

## ▶️ 6. Menjalankan Bot

Jika `index.py` sudah berada di folder yang benar, jalankan bot:

```sh
python index.py
```

---

## 📦 7. Mengumpulkan Semua Akun Hasil Farming

Gunakan perintah berikut untuk menggabungkan semua akun:

```sh
python gather.py
```

---

## ✔️ Selesai

Bot sudah siap digunakan 100%.
Jika ingin menambah fitur, otomatisasi lain, atau ingin dibuatkan versi lebih sederhana, tinggal bilang!

